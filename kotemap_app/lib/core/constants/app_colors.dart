import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Couleurs primaires (bleu marine KOTE MAP)
  static const Color primary = Color(0xFF1A3E6E);
  static const Color primaryLight = Color(0xFFF0F5FF);
  static const Color primaryDark = Color(0xFF0F2444);

  // Transports
  static const Color bus = Color(0xFF1A3E6E);
  static const Color taptap = Color(0xFFEA6518);
  static const Color moto = Color(0xFF7C3AED);

  // Statuts
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFD97706);
  static const Color danger = Color(0xFFDC2626);
  static const Color info = Color(0xFF3B82F6);

  // Zones de danger
  static const Color dangerZone = Color(0x1AEF4444);
  static const Color dangerZoneBorder = Color(0x66EF4444);

  // Fond
  static const Color background = Color(0xFFF2F2F7);
  static const Color surface = Colors.white;
  static const Color surfaceSecondary = Color(0xFFF8FAFF);

  // Textes
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF8E8E93);
  static const Color textDisabled = Color(0xFFC7C7CC);

  // Bords
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFF2F2F7);

  // Itinéraires — tags
  static const Color tagFastBg = Color(0xFFDBEAFE);
  static const Color tagFastText = Color(0xFF1E40AF);
  static const Color tagSafeBg = Color(0xFFFEF3C7);
  static const Color tagSafeText = Color(0xFF92400E);
  static const Color tagCheapBg = Color(0xFFDCFCE7);
  static const Color tagCheapText = Color(0xFF166534);

  // Alerte
  static const Color alertBg = Color(0xFFFEF3C7);
  static const Color alertBorder = Color(0xFFFCD34D);
  static const Color alertText = Color(0xFF92400E);
  static const Color alertDot = Color(0xFFD97706);

  // User location
  static const Color userDot = Color(0xFF3B82F6);
  static const Color userRipple = Color(0x263B82F6);

  // Modération
  static const Color pendingBg = Color(0xFFFEF3C7);
  static const Color pendingText = Color(0xFF92400E);
  static const Color validBg = Color(0xFFDCFCE7);
  static const Color validText = Color(0xFF166534);
  static const Color warnBg = Color(0xFFFEE2E2);
  static const Color warnText = Color(0xFF991B1B);
}
